## Upstream PKGBUILD update: `${PKG_NAME}`

${SOURCE_INFO}

### Diff

```diff
${DIFF_OUTPUT}
```

---
⚠️ **Review carefully before merging.** Check for:
- Modified `source=` URLs or checksums
- New/changed `install` hooks
- Added `post_install`/`pre_remove` functions
- Unexpected new dependencies
- Any scripts run as root